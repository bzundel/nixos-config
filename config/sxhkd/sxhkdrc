super + Escape
	pkill -USR1 -x sxhkd

XF86Audio{Lower,Raise}Volume
	wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%{-,+}

XF86AudioMute
	wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

super + space
	setxkbmap -query | grep -q '^layout:.*us' && setxkbmap de || setxkbmap us
